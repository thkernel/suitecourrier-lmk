



// Pass jQuery element
  //const choices = new Choices($('.customSelect'));

  // Passing options (with default options)
  //const choices = new Choices(element, {});

// const element = document.querySelector('.customSelect select');
//  const choices = new Choices(element);
/*
  document.addEventListener("turbolinks:load", function() {
    var dropDownSelects = new Choices('.form-select')
    var choices = new Choices($('#support_id'),{
      searchEnabled: true,
    focusState: 'is-focused',
    duplicateItemsAllowed: false,
    maxItemCount: -1,
    removeItemButton: false,
    position: 'auto',
    placeholder: true,
    placeholderValue: 'Please Select Choices',

    });
    
})*/