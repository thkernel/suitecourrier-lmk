$("ul.errors").html("");
<% if @mail_priority.errors.any? %>
  <% @mail_priority.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".mail-priorities").html("<%= escape_javascript(render 'index') %>");
  $("#mail-priority-modal").modal("hide");
 
<% end %>