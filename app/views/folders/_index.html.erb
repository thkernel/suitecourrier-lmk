<% @folders.each do |folder| %>
    <tr>
    
    <td><%= folder.name %></td>
   
   <td>
	    <div class="action-buttons">
			<%= link_to raw('<i class="fa fa-pencil" aria-hidden="true"></i>
'),  edit_folder_path(folder), title: "Modifier", remote: true %>




<%= link_to raw('<i class="far fa-trash-alt"></i>
'), folder_delete_path(folder), remote: true, title: "Supprimer", class: "delete" %>
</div>
</td>
    </tr>
<% end %>