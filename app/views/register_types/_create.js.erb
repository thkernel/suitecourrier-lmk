$("ul.errors").html("");
<% if @register_type.errors.any? %>
  <% @register_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".register-types").html("<%= escape_javascript(render 'index') %>");
  $("#register-type-modal").modal("hide");
 
<% end %>