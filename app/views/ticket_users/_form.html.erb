<%= form_with(model: ticket_user, local: true) do |form| %>
  <% if ticket_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ticket_user.errors.count, "error") %> prohibited this ticket_user from being saved:</h2>

      <ul>
      <% ticket_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :uid %>
    <%= form.text_field :uid %>
  </div>

  <div class="field">
    <%= form.label :ticket_id %>
    <%= form.text_field :ticket_id %>
  </div>

  <div class="field">
    <%= form.label :recipient_id %>
    <%= form.text_field :recipient_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
