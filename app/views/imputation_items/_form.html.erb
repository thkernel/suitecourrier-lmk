<%= form_with(model: imputation_item, local: true) do |form| %>
 
<div class="modal-body">
    <ul class="errors">
    </ul>
 
  <div class="form-group">
    <%= form.label :task_type_id, "Type", class: "required" %>
    <%= form.collection_select :task_type_id , @task_types, :id, :name, {prompt: "Sélectionner"}, class: "form-control", style: "width: 100%", readonly: true %> 
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description, class: "form-control", readonly: true %>
  </div>

  <div class="row">
    <div class="form-group col-md-6">
      <%= form.label :due_date, "Date d'échéance" %>
      <%= form.date_field :due_date, class: "form-control", readonly: true %>
    </div>

  

    <div class="form-group col-md-6">
      <%= form.label :task_status_id, "Progression", class: "required" %>
      <%= form.collection_select :task_status_id , @task_statuses, :id, :name, {prompt: "Sélectionner"}, class: "form-control", style: "width: 100%", required: true %> 

    </div>
  </div>

 
  </div><!-- modal body -->
  <div class="modal-footer">
    <%= link_to "Annuler", "#", class: "btn btn-secondary tx-mont tx-medium tx-11 tx-uppercase pd-y-12 pd-x-25 tx-spacing-1", data: {dismiss: "modal"} %>
    <%= form.submit  class: "btn btn-primary tx-mont tx-medium tx-11 tx-uppercase pd-y-12 pd-x-25 tx-spacing-1",  "data-loading-text"=>"Loading...", autocomplete: "off" %>
  </div>
<% end %>
